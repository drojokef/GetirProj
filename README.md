# GetirProj
Getir Project

Pre-Requests:
-NodeJS

run command:
-npm install

Then run command;
-npm start

Enter this url : http://localhost:8080/getir/records
Use POST metod for request data.

For Test;
-npm test
